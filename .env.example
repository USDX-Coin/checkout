# Base URL backend USDX (endpoint mint v2). Origin harus diizinkan CORS backend
# (`CORS_ORIGINS`). dev: https://api-dev.usdx.co.id ; prod: https://api.usdx.co.id
NEXT_PUBLIC_API_BASE_URL=https://api-dev.usdx.co.id

# URL `app` consumer — tujuan redirect saat 401 (token absen/kedaluwarsa). Auth checkout =
# bearer JWT di-handoff via URL hash dari `app` (USDX-239). Isi per-env di Netlify UI
# (mis. https://app.usdx.co.id). Kosong di localhost → fallback `router.back()`.
NEXT_PUBLIC_APP_URL=

# DEMO ONLY (dev/preview): status tracker disimulasikan maju ke "Selesai" setelah bayar,
# karena pipeline on-chain real (Auto-Propose → Safe) belum jalan di dev. JANGAN set di prod.
NEXT_PUBLIC_DEMO_AUTOCOMPLETE=true
